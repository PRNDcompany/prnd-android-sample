rootProject.name = "groovy"
include ':app'

enableFeaturePreview("VERSION_CATALOGS")

dependencyResolutionManagement {
    versionCatalogs {
        deps {
            from(files("gradle/dependencies.toml", "../dependencies.toml"))
        }
    }
}
